sudo: required
services:
  - docker

before_install:
  - docker build -t docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true docker-react npm run test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "docker-react"
  env: "Dockerreact-env"
  bucket_name: "elasticbeanstalk-us-east-1-449527814956"
  bucket_path: "docker-react"
  on:
    branch: master
  access_key_id: "AKIAWRKPSDMWMFT5JS6S"
  secret_access_key:
    secure: "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"